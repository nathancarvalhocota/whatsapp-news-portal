# Samples — Fixtures para Modo Demo

Esta pasta contém arquivos HTML e JSON usados como fixtures pelo modo demo do pipeline editorial.

O modo demo utiliza esses dados estáticos para simular o fluxo completo (ingestão, classificação, geração e publicação) sem depender de fontes externas ou da API do Gemini.
